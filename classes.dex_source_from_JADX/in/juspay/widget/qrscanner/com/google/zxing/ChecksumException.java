package in.juspay.widget.qrscanner.com.google.zxing;

public final class ChecksumException extends ReaderException {
    private static final ChecksumException f2654c = new ChecksumException();

    static {
        f2654c.setStackTrace(b);
    }

    private ChecksumException() {
    }

    public static ChecksumException m4637a() {
        return a ? new ChecksumException() : f2654c;
    }
}
